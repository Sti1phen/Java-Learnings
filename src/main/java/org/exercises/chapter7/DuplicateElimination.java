package org.example.chapter7;
/*
declare an array with length equals 5
Prompt user to input number between 10 and 100
while number is less than 10 or more than 100,
    try again until user type correct value
For count = 0 , less than array length,
If number != array[count]
Print number


for array element 1 till 5
    Prompt user to input the user for a number between 10 and 100
    Input user response as number[count] = input.nextInt()
    if number[count] ==
    

 */